import Form from "./Form"

function Layout({children}) {
    return (
        <div className="layout-container">
            {children}
            {/* <Form/> */}
        </div>
    )
}

export default Layout
